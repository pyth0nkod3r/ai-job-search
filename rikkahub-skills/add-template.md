---
name: job-search-add-template
description: >
  Registers a custom CV or cover letter LaTeX template so /apply can use it alongside the
  built-in moderncv/cover.cls. Triggers on: /add-template, add cv template, add cover letter
  template, register template.
---
# Add Template (thin pointer)

Read and follow `/workspace/ai-job-search/.claude/commands/add-template.md`. Tool mapping per
`/workspace/ai-job-search/RIKKAHUB.md`. Repo root: `/workspace/ai-job-search`.
`$ARGUMENTS` = the user's message.
